version: '3.8'

services:
  app:
    image: python:3.11-slim
    volumes:
      - .:/app
    working_dir: /app
    command: sleep infinity

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"